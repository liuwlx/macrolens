# ML-20260803-002 生产部署任务卡

- 来源主线程：当前 Codex 主线程 `/root`
- 目标与业务场景：清理 `111.229.152.122` 上与 MacroLens 无关且可安全停止的业务应用，并把当前 `main` 分支部署为可完整运行、可外网验收的 MacroLens 实例。
- 成功标准：服务器保留 SSH、防火墙、容器运行时等基础设施；MacroLens Web、API、Worker、Scheduler、PostgreSQL、对象存储正常；数据库迁移和种子数据完成；外网 Web 与 API 健康检查通过；提供可点击验收链接及回滚说明。
- 范围内：服务器只读盘点、备份现有业务容器/反向代理配置、停止明确可替换的旧业务应用、配置生产环境变量、构建和启动 MacroLens、反向代理或端口发布、冒烟与关键路径验收。
- 范围外：删除服务器数据卷、删除未知来源文件、修改云厂商安全组、购买或变更域名、生产级第三方 API 密钥配置。
- 分配部门席位：运维部 01、安全合规部 01、测试部 01、集成发布部 01、研发部 01（生产构建阻断修复）。
- 工作树与起始提交：本地项目根目录；`79eca99e8752a0e467856bf02b971e40e7eac6fb`。本任务以部署和文档为主，不在部门线程修改业务代码。
- 允许修改的模块：部署配置、运行环境配置、`docs/conclusions`；生产构建阻断确认后，允许研发部修改 `apps/web/package.json`、根 `package-lock.json`，并对 `next build` 逐项暴露的 TypeScript 编译错误做最小类型安全修复；不得跳过类型检查或扩大到无关重构。
- 公共接口或 Schema 影响：预期无。
- 依赖任务：无。
- 必须执行的检查：项目规定的后端与前端检查；Docker Compose 配置校验；服务器端容器健康检查；API ready；Web 首页、登录或演示关键路径；从外网访问验收链接。
- 预期交付物：运行中的部署、验收链接、服务/端口清单、部署与回滚记录、结论报告。
- 阻塞时返回条件：无法建立 SSH 连接；无法辨认待停应用归属；缺少必要生产密钥且无安全默认替代；服务器资源或端口不足；必须修改安全组或域名但未获授权。
