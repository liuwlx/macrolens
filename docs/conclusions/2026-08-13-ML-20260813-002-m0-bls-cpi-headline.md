# ML-20260813-002：BLS CPI Headline M0 真源纵向样板

- 任务：GitHub Issue #9
- 角色：ML｜研发部｜04（PRIMARY）
- 基线：`9386941517a5a9c385a1349ba706941ef21cdc34`
- 分支：`codex/ML-20260813-002-m0-cpi-headline`
- 工作树：`E:/workerspace/projects/20260709/macrolens-tasks/ML-20260813-002`
- 最终状态：`BLOCKED`（代码候选可供集成复核；真实 BLS 与 PostgreSQL 验收证据受当前环境阻塞）

## 1. 问题与场景

现有 BLS adapter 能读取数据，但缺少可持久审计的 MappingProbe、单 source scoped sync、探测证据绑定的原子 primary 晋级、断网 raw replay，以及逐值贯通 API/导出的 run、batch、raw 和 source lineage。BLS `registrationkey` 还会进入请求元数据与 raw bundle；每次变化的 `responseTime` 和采集时钟会让相同业务响应形成重复 raw/vintage。registry seed 也可将 `READY` 直接晋级 verified/primary，绕过探测。

## 2. 分析过程

先完整读取组织合同、实施命令、项目计划、Issue 02 与工单 02，再从 `US.CPI.HEADLINE` registry 映射沿 BLS adapter、Worker runner、sync、raw/run/batch/vintage/latest 模型及 series/browser/export API 逐层核对。RED 阶段新增聚焦测试，初始为 `5 failed`，暴露 probe/replay 缺失和密钥持久化。实现后又通过双轴审查识别出 volatile `responseTime` 幂等、seed 绕过、CSV 兼容、失败探测结构、审批重试、身份漂移、dataset 错配及 vintage 时间身份问题，并逐项修复。

现场只读探测明确区分了环境层次：`BLS_API_KEY` 未配置；PowerShell 最小 POST 约 10.3 秒后 WebException；Python/httpx 最小匿名 POST 约 8 秒后 TLS `UNEXPECTED_EOF_WHILE_READING`。因此本机不能形成真实 Worker 出口的 HTTP/业务/授权生产证据。官方 BLS 文档用于校验 `CUSR0000SA0` 身份及 fixture 值来源，但未冒充 live probe。

## 3. 解决工作流

1. 在 Worker Provider 内实现 BLS 严格业务成功、响应覆盖、series ID、catalog title/字段 identity 判定；持久化前移除密钥与 volatile telemetry。
2. 新增结构化 MappingProbe：分别记录 transport、HTTP、业务、identity、credential、production-ready；失败同样返回证据，晋级必须满足完整 PASS。
3. 将批准证据通过唯一 `verification_job_id` 和 mapping fingerprint 绑定 SourceSeries；身份变化、禁用或许可阻塞会撤销；已消费 probe 不可重试覆盖；原子 demote/promote 并由部分唯一索引兜底。
4. Worker 支持单 source scope；重复业务 raw 得到稳定 SHA，同 raw 重放不新增 vintage；BLS 每 dataset 使用统一 captured_at，raw 与 observation vintage 可精确复现。
5. 新增 `replay_bls_raw` Worker job：从不可变 raw object 读取，核验 bucket、SHA、provider、dataset 和 source scope，零外网调用复用生产 parser。
6. 为 observations、revisions、analytics、browser current/previous 和 CSV 导出增加 source/run/batch/raw lineage；新 CSV 列追加于旧列末尾，保留旧列前缀。
7. 生成并校验 OpenAPI；补充脱敏 CPI fixture、Provider/Worker/审批/幂等/重放/API 合同测试；离线生成迁移 SQL但未应用迁移。

## 4. Agents、skills、tools 与文档

- Agents：主执行席位 `ML｜研发部｜04`；独立只读 `standards_review_m0` 与 `spec_review_m0`，以 base `9386941` 为固定点多轮复审。
- Skills：`tdd` 用于 RED→GREEN→REFACTOR；`code-review` 用于 Standards/Spec 双轴审查。最终两轴均为 `0 P0 / 0 P1 / 0 P2`。
- Tools：Git/worktree、PowerShell、Python 3.12、pytest、Ruff、mypy、Alembic offline SQL、OpenAPI generator、repository validator、Node 24/npm、Vitest、Next build、BLS 最小 HTTP 探测。
- 文档：根 `AGENTS.md`、`.codex/organization.toml`、`docs/organization/README.md`、implement-command、project-implementation-plan、Issue 02、work order 02、BLS 官方 API signature/series identity/CPI archive。

## 5. 检查、经验与风险

### RED / GREEN

- RED：新聚焦测试初跑 `5 failed`；OpenAPI `--check` 报 out of date。
- GREEN：`backend/tests/test_m0_bls_cpi.py` 为 `14 passed`；与 live-availability 受影响测试合跑 `27 passed`；聚焦新增/核心 Python 文件 Ruff 全绿。
- 全后端：最终将新增两项 admin 回归计入后为 `160 passed, 1 failed, 5 warnings`。唯一失败 `test_registry_and_schema.py::test_api_route_surface` 来自当前 FastAPI `_IncludedRouter` 无 `.path` 的既有测试/依赖兼容问题，不在本票改动路径。
- 全 Ruff：失败，`319 errors`，为仓库既有格式/长行/未使用导入基线；聚焦文件 Ruff 通过。
- 全 mypy：失败，`35 errors in 16 files`，为既有 typing/stub 基线；本票 sync 新增的 Row 类型问题已修复。
- Web lint：失败，`52 errors, 4 warnings`，均为既有 React effect 与 E2E `no-explicit-any`；改动的 `lib/types.ts` 无新增 lint 错误。
- Web test：`10 files / 32 tests passed`。
- Web build：Next.js 16.2.12 构建成功，15 条 route；自动生成文件已恢复，不纳入提交。
- OpenAPI：`70 paths`，`--check` 通过；repository validator：`61 source series, 70 API paths` 通过。
- Alembic：`upgrade head --sql` 成功生成 0001→0002 SQL；遵守授权未应用迁移。
- `git diff --check`：通过（仅 CRLF 提示）。

经验：生产可用不能由 HTTP 200 推断；映射批准必须绑定“具体身份字段的指纹 + 不可覆盖证据”；幂等不能只看 value，应消除 provider telemetry 对 raw identity 的影响；offline replay 必须共享 raw 的 captured_at、provider、dataset 和 SHA；新增 CSV 字段应保留已有列前缀；seed 是生产门禁的一部分，不能作为特权旁路。

风险与解锁条件：当前无 BLS key 且官方端点 TLS 失败，无法验证真实 PASS/授权；Docker/PostgreSQL 不可用，未运行真实 DB 的唯一 primary、并发切换、事务回滚与 raw→run→batch→vintage→latest→API 端到端。解锁需在允许访问 BLS 且安全注入正式 key 的 Worker 环境执行最小 probe，并在临时 PostgreSQL/S3 环境应用 0002 后跑双次 scoped sync、API/导出与重放验收。迁移未应用、未 push、未部署、未重启、未改密钥。GitHub Issue #9 保持开启。

## 6. 更好的初始提示词

> 请在指定本地隔离 worktree 中为 `US.CPI.HEADLINE` 建立 BLS 官方真源纵向样板。先写失败测试，再实现：结构化分层 MappingProbe；证据与 mapping fingerprint 绑定的原子唯一 primary 批准；单 source scoped sync；脱敏且去除 volatile telemetry 的稳定 raw；raw/run/batch/不可变 vintage/事务性 latest 的双次运行幂等；raw SHA/provider/dataset/scope/captured_at 绑定的零网络 Worker replay；browser/detail/observations/revisions/analytics/export 每个值的 source/run/batch/raw lineage。禁止 seed、admin 或重试绕过批准，身份变化必须撤销验证。使用临时 PostgreSQL/S3 跑真实双次 sync 与 API 端到端，并单独记录 BLS transport、HTTP、业务、identity、credential、production-ready。运行全量门禁与双轴 code review，修复 P0-P2，生成七段报告和单一候选提交；不应用生产迁移、不 push/deploy。

## 7. 更优方案反思

当前方案在既有模型上最小扩展，适合集成，但真实 PostgreSQL 证据依赖外部环境。更优方案是提供仓库内可重复的 M0 acceptance harness：启动临时 PostgreSQL 与对象存储，用 MockTransport 捕获两次仅 telemetry 不同的官方形状响应；从同一入口依次执行 probe→approve→scoped sync×2→API/browser/export→raw replay，并断言唯一 primary、raw/vintage/latest 数量、事务回滚、逐值 lineage 和 replay identity。另设一条 opt-in live probe 只验证 BLS 网络/授权，不写业务数据。

> 请新增一个可独立运行的 M0 acceptance harness：自动启动临时 PostgreSQL/S3，应用迁移，加载仅 CPI 映射，使用脱敏 BLS fixture 和可变 responseTime 完整执行 probe、批准、两次 scoped sync、全部内部 API/CSV 与 raw replay，逐项断言唯一 primary、幂等计数、captured_at、SHA 和 lineage；随后可选运行真实 BLS 只读 probe，并把 transport/HTTP/business/identity/auth/production-ready 分层输出。任何层失败均 fail closed 并生成证据报告，不修改生产数据或密钥。
